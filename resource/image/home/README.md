# Home Templates

Place cropped home/lobby templates here.

Expected files:

- `home_marker.png`: stable home-screen marker. Prefer the top-right home/settings icon group or a cluster of stable left-side function icons.

Avoid event background art, banners, timers, resource numbers, and notification badge numbers.

